# =============================================================================
# SubtitleRAG - Kubernetes Secrets
# IMPORTANT: Do NOT commit this file with real secrets!
# =======================================================================

apiVersion: v1
kind: Secret
metadata:
  name: subtitle-rag-config
  labels:
     app: subtitle-rag
type: Opaque
data:
  # Base64 encoded: echo -n 'your-api-key' | base64
  langchain-api-key: <placeholder>

# Create properly via kubectl:
# kubectl create secret generic subtitle-rag-secrets \
#   --from-literal=langchain-api-key=lsv2_pt_xxxx \
#   --dry-run=client -o yaml > secrets-real.yaml